    // // Validation
    //     let errors = {};
    // if (values.title.length < 5 || values.title.length > 20) {
    //   errors.title = "Title must be between 5-20 characters long";
    // }
    // if (!values.imgUrl.startsWith("http://")) {
    //   errors.imgUrl = "Image URL must start with 'http://'";
    // }
    // if (values.description.length < 20) {
    //   errors.description = "Description must be at least 20 characters long";
    // }
  
    // // Display errors to user
    // if (Object.keys(errors).length > 0) {
    //   alert("Please fix the following errors:\n\n" + Object.values(errors).join("\n"));
    //   return;
    // }
